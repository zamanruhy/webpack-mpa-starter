<%
  const styleguide = require('@/components/styleguide/styleguide.ejs')
  const example = require('@/components/example/example.ejs')
  const icon = require('@/components/icon/icon.ejs')
  const houseIcon = require('@/assets/svg/house.svg').default
  const inIcon = require('@/assets/svg/in.svg').default
  const closeIcon = require('@/assets/svg/close.svg').default
%>

<div class="container examples" style="display1: none;">
  <%= example({
    title: 'First',
    slot: `<div class="truncate">Достаточнодавноза&shy;нимаюсь разработкой на Vue.js, но вот упаковывать компоненты для публикации как то не приходилось.
    Недавно пришла идея интересного компонента, и я решил поделиться им с сообществом.</div>`
  }) %>
  <%= example({
    title: 'Second',
    showSlot: true,
    slot: `
      <app-button variant="primary" data-modal="main-modal">
        ${icon({ data: inIcon, attrs: { style: 'margin-right: 4px;' } })}
        Slot for second
      </app-button>
    `
  }) %>
</div>

<%= styleguide() %>

<div class="container" style="display: flex; justify-content: center;">
  <img
    src="<%= require('../assets/img/34A3721C-53F0-4371-ABB9-F7CB9C94F053_w1200_r1.jpg') %>"
    width="1200"
    height="675"
    style="width: 700px; height: auto;"
    loading="lazy"
    alt="#"
  >
</div>

<br>
